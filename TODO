- document functions
- sim error handling
- update raw_data parameters
- generic functions like norm
- load data from sims
- run baseline sim without parameter changes for each iteration
  - option to do so
  - return objective
  - track objective over iterations
- apply variants before sim_data changes
- add status tracking
  - store objective and params over time
  - plot objective at each iteration
  - save stats to spreadsheet

considerations:
- create workflow dependencies to more easily scale
